# 人工黄金边界的离线特征研究

本研究只为解决最终裁切的内切、外扩超标或安全批准缺口。人工黄金保护线指导进入原 TIFF 有界邻域，
包含检测器尚未发现的画面边界；研究区分真实接触与竞争解释，但不要求解释完全部像素现象。模型预测
相符、局部梯度最大或成功样片上常见的特征不能直接成为边界权限。运行时继续使用现有放置模型与联合
搜索，黄金坐标只用于离线研究和评价。

## 验收对象与停止条件

- H 是否解决由最终 post-bleed 裁切决定：保护画面、留边预算合格且整张可直接使用。检测线距人工线
  几像素不是失败条件，人工线也不是精确到 1 px 的接触真值。
- 允许多个合法 H 解释。完整保留它们的位置、方向、证据身份及相互关联，由现有联合求解检查是否存在
  一份覆盖全部解释、对每个解释都安全且不过量外扩的输出。无需为了唯一边界消除无害的不确定性。
- 只有不确定性已阻碍合格输出时，才继续细化接触证据。已直接可用的样片停止新增精度机制，但仍保留在
  全部回归中；已测到正确接触但输出预算或联合投影失败的样片，先检查相应输出 owner。
- 新特征、缩窗、复杂搜索必须改善真实样片的安全输出或解决具体阻断，并接受反例及回归验证；仅增加峰、
  增加解释细节或接近人工线，不构成运行时集成依据。下文的描述性发现不是一份必须逐项实现的清单。
- 黄金保护、既定外扩预算、错误通过为零及性能目标不变。H 验收之后才推进 W；W 还须证明照片归属及
  间隔、接触、重叠、截断关系正确。正式性能验收仍留在发布阶段。

### 按最终输出重分开发任务

在干净提交 `7283bce6` 上，以当前本机依赖重新执行全部 110 个任务、106 个扫描源的完整生产流程，
逐源校验报告与原 TIFF SHA，并用既定黄金 comparator 评价最终裁切。110 项全部完成，运行及报告
校验错误为 0；96 项 nominal 中 20 项安全自动通过，14 项 challenge 均保留 Review，错误自动通过为 0。
全部来源均参与过开发，这不是未见来源验证、H 整体验收或正式性能验收。

| 输出层面的后续事项 | nominal | challenge | 处理边界 |
|---|---:|---:|---|
| 已安全自动通过 | 20 | 0 | 停止 H 精度研究，保留回归 |
| primary 的 H 几何、运行时预算及 Cross 已通过，阻断在其它部分 | 14 | 1 | 不新增 H 接触机制；另查 W、离散归属及其它实际阻断 |
| 黄金 H 几何通过，但运行时 H 预算失败 | 26 | 5 | 查明实际输出扩展及预算证明缺口，不放宽预算 |
| H 内切或黄金外扩预算失败 | 23 | 7 | 研究能直接改善这些输出的几何或接触缺口 |
| 黄金 H 几何和运行时 H 预算通过，但 Cross 多解或权限未闭合 | 8 | 1 | 检查完整解释集的共同输出或权限证明 |
| H 投影范围包住黄金，但联合角边半平面仍失败 | 4 | 0 | 保留裁切失败，先检查 W/H 联合角点，不直接归因于 H 接触 |
| 长轴模型不相容，未生成 primary 输出 | 1 | 0 | S051 归入长轴闭合问题，不增加 H 精度机制 |

Review 项的分类使用保留 primary 的实际 footprint，不通过黄金标签选择其它候选。
S052、S073、S079、S090 属于联合角边类；S051 的 Cross 已闭合，但授权的长轴直接坐标无法完成有界
格距重拟合，因此缺少 primary；已保留的 runner 可以物化，不是 H 顶点投影超界。黄金 H 几何通过不代表运行时预算、
完整权限及整源批准也通过，不能把这些层次合并统计。

完整结果与逐帧分类位于 `Test/gold_analysis/h_common_selected_commit_20260923/` 的
`gold_analysis_records.jsonl`、`gold_analysis_summary.json` 与 `output_triage.json`，重算脚本为
`reclassify_outputs.py`。历史开发基线的 16 项安全自动通过全部保留，新增 S067、S069、S078、S082；
该比较不是直接父提交的重放，不能把四项差异都归因于共同 H 输出。S067 是本轮已验证的共同输出收益，
其余三项使用已解析的原生 H 路径。

### 已整合的共同 H 输出与反例边界

现有联合求解在 W 归属已固定、上游归属与投影完整时，保留正式及条件视图中的全部不同 H 解释。
共同输出覆盖各解释的完整 mandatory/requested 范围，按每个成员重新检查四边预算、源域、角色权限、
空间覆盖、高度相容性及 enclosing-support 竞争，并对实际共同裁切检查内容反证、最终 deskew 与采样范围。
它具有独立输出身份及预算证明，不借用 primary 的权限，也不把 Cross 多解改写为唯一解。
单个条件绑定仍不因此成为可独立批准的原生候选。具体合同与 owner 见 [ARCHITECTURE.md](ARCHITECTURE.md)。

- S067 保留 18 个不同 H 解释，共同裁切的 3 帧均满足黄金保护及全部成员预算，最坏预算使用为 73.9%；
  完整生产运行安全自动批准，Cross 仍为 `non_equivalent_fits`。无需继续追求唯一接触线。
- S035 的正式视图包含支持不足组，S048 缺少完整空间覆盖权限，不能由合并候选获得批准。两者保持 Review。
- S058 的当前原生候选通过黄金几何，但运行时直接使用预算失败，仍为 Review。此前固定解释的离线共同
  裁切也超预算；共同输出不是绕过实际扩展成本的手段。
- S016 的不安全提案仍被阻断。人工确认的错误局部蓝线与准确的 S003/S047/S064 蓝线保持原有解释，
  不用检测线到黄金线的距离代替最终输出安全性。

反例覆盖比例高度冲突、enclosing-support 竞争、成员或 raw union 遗漏、局部投影不完整、内容否决、
逐成员预算超限，以及报告借用原生身份、篡改共同范围或最终采样的情况。完整上游解释不能只由 aggregate
`COMPLETE` 推断；必须逐 scope 核对，并确认每个 maximal raw union 已进入实际求解输入。
证据不足保持 Review，不删除竞争解释来制造唯一性。

### 仍会影响输出的研究范围

31 项运行时 H 预算失败来自 30 个扫描源，primary 全部使用 `enclosing_support_pair`，中心校准均已
获权，支撑 span 上限均通过，cross bleed 与 topology 保护均为零。共 226 个上下边逐侧预算超限，
其中 14 项另有同状态 alignment 超限；这 14 项全部同时逐侧超限，单独改变 alignment 不能解锁输出。
预算 Gate 实际执行并阻断的为 12 项，其余 19 项仍有上游阻断；不得将全部 proposal 数值失败称为已执行的 Gate 失败。

在干净提交 `fe56f5e7` 的完整 CLI 中只读捕获 S012、S098、S058 的同状态风险输入，不改变函数返回值；
报告通过当前校验，保留 primary 的 footprint 及决定与全量基线完全一致。逐状态展开支撑余量、合法中心偏移与局部保护后，得到以下反例：

| 样片 / 帧 / 边 | 当前风险 / px | 保留现有输入合同的必要需求 / px | 上限 / px |
|---|---:|---:|---:|
| S012 / 5 / TOP | 114.707 | 域内登记区间端点至少 108.982 | 100.650 |
| S098 / 10 / TOP | 160.842 | 域内登记区间端点至少 147.320 | 100.650 |
| S098 / 10 / BOTTOM | 138.788 | 域内登记区间端点至少 124.890 | 100.650 |
| S058 / 1 / TOP | 114.820 | 支撑余量与中心偏移合计 113.820，另有 1 px 保护 | 100.650 |

前两源的端点位于相应状态的实际输出长轴范围内。即使不把该点的 departure 平移到整条支撑线，
它相对同一合法 aperture 解释的距离仍已超限；更紧的多边形不足以让这两项通过。S058 每侧局部保护
只有 1 px，削减 residual 同样不能解决。不能凭黄金几何通过直接扣减这些代价。

进一步核对同一组捕获输入：S012 第 5 帧和 S098 第 10 帧的最坏 TOP，各自都有一个完整保留的
raw 端点，沿获准方向外推到该状态的实际输出右端后，所需保护恰好等于运行时风险，分别为
114.706903 和 160.841987 px，重算误差小于 `1e-7` px。这两项不是不同位置的最坏量错误叠加；
只要仍须覆盖该获准端点，改成局部折线或更紧凸包也不能减少这两项风险。

这里的“登记区间”是当前测量与输出合同，不是新确认的逐像素真实接触。支持直线约束只要求直线
到区间的最近距离不超过 `straight_model_residual`，不表示区间内所有真实位置都距直线如此近。
S098 的同一合法状态在最后一个 TOP trace 上取 354.5 px，残差为零，而 raw 区间仍为
`[281.5,354.5]`；直接裁成直线邻域会删除 73 px 的既有保护权限。缩小这类集合需要新的独立像素证据
或可证明的测量相关性，不能从状态合法或残差为零循环推出。S012/S098 的所选支撑为 `sharp_transition`，
S058 为 `broad_material`；不能把它们的测量来源混为一类。证据为 `Test/gold_analysis/h_support_risk_witness_20260923/` 的
`audit.py`、逐源 `*_witness.json` 与 `case_audit.json`，正式诊断报告位于相应
`Test/debug_analysis/h_support_risk_witness_20260923/`。此检查不改变运行时或扩大预算。

### 实际阻断与接触观察的对应关系

上述三源的有界原 TIFF 窗口均可观察到外侧亮材料、暗边带和内侧画面的多层变化。原始 16 位 RGB、
源 SHA、窗口范围与仅供显示的拉伸分别保存在
`Test/gold_analysis/h_contact_witness_pixels_20260923/`；该观察不新增接触 reference。
将窗口与同源完整 CLI 的原始 transition、登记 observation、角色和联合求解结果对照后：

| 来源 | 内侧邻域的已测证据 | 真正需要继续处理的阻断 |
|---|---|---|
| S012 | x=15255 的 TOP y=296、BOTTOM y=2397 均已有 transition | TOP 普通线具备两个区域，但外侧 texture 投票为 0.4722，未获角色权限；条件 family 线有角色权限但归属未闭合。正式直接 H 组的纵向权限只覆盖 1–2 帧，不能代表全部 6 帧 |
| S098 | x=15030 的 BOTTOM y=2490.5 已测；TOP 内侧邻域在相邻 x=14850/15210 已有登记区间，该 trace 本身缺少对应独立峰 | 已有局部上下配对，但长轴 phase 未闭合，全部直接 H 组缺少 template domains；不能把它归为单一 H 漏检 |
| S058 | x=5265 的 TOP y=314、BOTTOM y=2433.5 均已有 transition | 对应 TOP 线只有一个独立区域，BOTTOM 的外侧 texture 投票为零；其它四组直接 H 虽获独立支持，合并后的既定输出仍超预算 |

当前 background 指标是左右 texture 大小投票，不是颜色或材料身份。上述观察不能授权降低
0.5 角色阈值，也不能把亮/暗顺序直接改成接触权限。S058 的四组直接解释因多解进入 unresolved
分支，随后由唯一 enclosing support 产生已解析结果；这解释了其表面上没有 Cross 歧义的报告状态，
不意味着其它直接解释已被像素排除。三源的 membership 均为 `search_bound_exceeded`，已有组不构成
证明完整的解释集。仅改变共同输出触发条件仍缺少完整性、权限与预算证明，不据此增加运行时路径。

这三项的继续研究以改善合格输出为条件：S012 先查角色与完整归属，S098 先区分长轴域缺失和局部
接触缺测，S058 先查解释集完整性与实际扩展需求。已测到的峰不重复增加检测机制；上述局部坐标也不作为
必须复现的精确目标。灰尘、片夹边、内部强线和突出点仍须在原有竞争解释中保留，不能凭更接近黄金线排除。

对局部精修的只读反例研究表明，“唯一最近”投影可能跳过竞争峰，剩余位置仍拟合成具备三个区域支持的线；
其工作凭据可与竞争位置完全缺测时相同。同一有界 H 内，两条平行实测线各自均可通过 Cross，最近选择却
只输入其中一条。原 raw ledger 尚在，缺口位于求解输入投影。复现位于
`Test/gold_analysis/h_contact_assessment_20260921/refinement_projection_audit.py` 及同名 JSON。
这是 registration/Cross 层反例，不是危险自动批准的端到端证明；当前共同输出要求逐 scope 完整性，
不能将未拟合竞争当成没有竞争。S067 不依赖该精修路径。

下一步以本次逐源失败清单为准：先定位真实输出扩展、缺失投影及未闭合权限的原因；只有确认接触缺口
阻碍安全合格输出，才继续新增特征或调整测量窗口。已经可用的 20 项保留回归，不增加精度机制。
先完成 H 验证与验收，再推进 W 实现；准确性、reference 权限与发布验收仍以
[ARCHITECTURE.md](ARCHITECTURE.md) 第 14 节及 [MANUAL_ANNOTATION.md](MANUAL_ANNOTATION.md) 为准。

## 数据与证据分层

- 按 `source_sha256` 聚合全部 count、Frame、H/W 物理线和局部窗口。同源任务不得进入不同开发分组，
  同一共享 H 线不能因跨越多张照片而成为多份独立证据。Frame 身份为同源 start/end line ID 对，不能
  跨 count 使用 ordinal 代替物理身份。
- `directly_visible`、`visible_content_limit` 和 `human_width_estimate` 分别统计。后两类不能混入
  清晰边缘正例；估计线附近偶然出现强梯度也不能反向改变人工依据。Blank 没有可见边界，不补造样本。
- 人工 line 没有逐位置可见性标注。由四条物理线交点得到的是几何采样范围，不代表整个范围都能看见
  边缘。保留逐位置数据、空间缺测和沿线覆盖，不能用平均值掩盖短而可靠的证据。
- 源截断处保留物理线和采样缺测；TIFF 外无像素，不向内钳位，也不把 TIFF 外缘补成真实成像边。
- 当前黄金集均属开发数据。按源确定的五组仅供后续开发比较，不能把重新分组包装成未见来源验证。
  未来独立封存样片遵循既有验收合同。

## 原图邻域测量

唯一离线入口：

```bash
tools/verify diagnostic --gold-boundary-features --axes HW \
  --output-root Test/gold_analysis/gold_boundary_features
```

`--axes H` 是默认值；`--sample-id` 可重复指定，但选择任一 task 都会纳入它的完整 source group。
执行前只读核对全部当前确认记录、审阅凭据、原 TIFF SHA 与 tracked cohort；不执行黄金提升。
输出限于忽略的 `Test/`，记录 cohort、源码、源图及产物哈希和依赖版本，不构成检测或性能 receipt。

每个非空 Frame 的 H/W 物理边界段使用 24 个等距内部位置；每处按原图法向采样，向内为正。
法向半径为对应 Frame 跨度的 5%，限定为 16–256 像素；切向取五条相邻剖面。采样采用原 TIFF
像素的双线性插值，消除半像素取整随镜像方向改变的问题，完整插值邻域越界时明确缺测。
原始 16 位 RGB 除以 65535 保留数值，不做局部拉伸、ICC 变换或把 JPEG 当像素来源。

保留 RGB 剖面及固定偏移网格上多个尺度的内外灰度、RGB 差、对立颜色差、法向/切向纹理、
变化相对于局部起伏的比值、近远变化极性和外侧平滑性。尺度为可完整容纳的 2/4/8/16/32 像素窗口。
这些都是描述量，不是接触分数、材料身份或自动选线规则；固定网格也不穷尽任意位置与倾斜解释。
24 个固定位置与五条局部剖面不能证明整条边界的材料连通性，也可能漏过很短的可见证据。没有测到
显著变化只能描述此次采样，不能据此把人工直接可见线降为不可识别；这类位置需要原图局部加密检查。

## 已核实的特征解释限制

用户对 `bounded_competitors.png` 的局部判读确认：S047、S003、S064 的蓝线是准确接触位置，S016
的蓝线不准确。这说明相对冻结黄金线向内偏移并不必然侵入真实画面；当前研究不能把偏移方向当成
真实接触的正负标签。人工黄金线不具有精确到 1 px 的真实接触定位含义，但仍严格约束最终裁切
不得进入其内侧。三处蓝线准确不意味着可以把最终裁切直接放到蓝线上，也不意味着黄金基线错误。
用户此次接触位置确认仅覆盖所展示窗口，不外推为整条共享边或其它照片的确认，不改写冻结黄金坐标。

开发集 S016 的有界原图邻域存在亮外层、暗边带与画面内容的多层结构，蓝线落在画面内部。它可以
作为此次局部判读中的错误接触例子；其余三处不能再作为错误接触反例。黄金保护线为高精度接触
搜索提供可靠的有界邻域指引，不能要求其坐标本身的梯度最大，也不能把邻近最强梯度自动命名为照片边界。

这些案例只解释为什么单点强度和梯度排序不足以完成接触判定，不产生样片分支或运行时阈值。
统计比较仍需覆盖全部来源及低反差、灰尘、片夹边、内部强线和突出点的反例；当前稀疏采样不是
这些干扰的完整语义标注集。

### 二维纹理与材料邻域的实测边界

106 个源、212 条共享 H 线、1108 个 Frame 边界段的加密测量，在原有 24 个位置周围采集切向
32 像素宽的原图邻域。固定偏移处分别比较 8/16/32 像素方形的近外、近内、远外、远内材料；
记录 RGB 均值、法向和切向差分、二维结构的第二方向信号及能量比例。二维计算复用现有 occupancy
的四乘四子格公式，内部使用 0–255 灰度码单位，结果再归一化；缺测单元隔离，不能污染其它单元。
这仍是原生像素研究，采样格与运行时 registered gray 不同，不继承 occupancy 权限。

直接移植 occupancy 的“第二方向信号至少 3 码且能量比至少 0.1”，不能成为接触的必要条件：

- 即使允许每个位置独立选择最有利的偏移与尺度，212 条 H 线中仍有 119 条不足半数采样位置满足它。
  这只是固定网格中的乐观可用率上界，不证明任何共同直线、上下配对或空间覆盖合同已经成立。
- 在人工确认的局部蓝线处，S064 内侧的三个尺度均没有单元满足该条件；S047 在 16/32 像素尺度
  也没有。S016 的错误蓝线内侧反而存在满足条件的单元。因此强二维纹理既非必要条件，也不能单独
  授权接触。未占用必须保留中性，不能解释成画面外侧。
- S085 的 nominal 原图邻域呈现明显材料分界与平滑画面，S105 的 challenge 邻域只有弱变化及局部
  亮点。这些是继续检查弱证据和孤立干扰的研究窗口，不新增人工接触标注，也不把亮点自动命名为灰尘。

“内侧纹理大于外侧”或“跨边变化大于外侧近远变化”在几乎所有邻域的某些位置都能找到。
这种高可用率不证明区分能力，不能通过逐位置挑有利偏移拼出边界。后续必须检验同一位置与方向
解释下的材料连续性、独立空间支撑和上下 H 相容性，并保留其它合法解释。

研究数据及可重算脚本位于 `Test/gold_analysis/h_contact_dense_20260921/`，有效全源测量为
`all_sources_corrected/manifest.json`，汇总为 `material_neighborhood_summary.json`。
局部单元检查覆盖平坦材料、单方向强边、旋转不变性、批量与单独计算一致性，以及缺测隔离；
这不是生产检测验收。

同源 separator 材料也不能直接代表共享 H 外侧。另从两条直接可见 W 线包围的 separator 内部采得
2060 个材料单元；审查的 106 个扫描源中，4 源没有合格单元。接触、重叠、人工估计 W 和不完整
双侧归属不作为材料种子。
对用户确认的局部蓝线比较，S064 的外侧更接近同源 separator 颜色，而 S003、S047 的内侧反而更接近。
因此不能将“外侧必须像 separator”或“内侧必须不像 separator”设为接触必要条件；颜色相近也不等于
材料相连或身份相同。原图单元及比较凭据为 `separator_material_reference.json` 与
`separator_reference_comparison.json`，黄金坐标仅用于离线定位这些研究单元。

### 与现有检测链的核对

当前完整 CLI 的 S003/S047 诊断和原图源哈希已核对，结果位于
`Test/debug_analysis/h_contact_reviewed_20260921/`：

- S003 的准确局部接触进入 bottom observation，取得角色权限并与 top 组成直接 aperture pair；
  proposal 与 candidate 均满足现有黄金合同。局部峰、整条拟合线与最终保护裁切仍是不同层次。
- S047 的准确局部接触同样已被测到并取得角色权限；完整 family 重拟合失败时保留了原 observation。
  最终 cross 使用 enclosing pair，整源因 W 冲突保持 Review；其 proposal 还存在黄金外扩预算超限。
  不能把它当成接触检测漏检，也不能把局部蓝线准确当成整源裁切合格。

上述运行只生成诊断报告和图，不输出正式 TIFF，不构成全 cohort 回归、H 验收或性能合格证明。

同一完整 CLI 的 S079 提案有明显 W 位移，第一帧的两个约 0.009 px 角边被归到 H，其延长半平面
不能包含黄金顶点。长 H 边本身包住了对应黄金范围，但最终 footprint 仍不安全。因此，逐侧黄金失败数
不能直接用作 H 接触检测错误数。现有 comparator 保留全部短边与原判定，新增
`inward_failure_witnesses` 解释失败的具体输出边、越界顶点及垂距，不增加定位容差。
对既有 source-bound 开发记录的 110 个任务、222 份保留候选、1172 帧重放后，旧有 verdict 与逐帧
数值完全一致；这是固定 footprint 的比较器回归，不是当前 detector 的全 cohort 验收。
重放凭据位于 `Test/gold_analysis/h_contact_dense_20260921/output_witness_equivalence.json`。

### 源边测量范围与窗口敏感性

S102 的完整 registered query 覆盖源内走廊，但现有 `measure_trace()` 用完整双侧 texture 窗口裁小
所有通道的共同坐标域。21 px 窗口、5 px 间隔、1998 px 源高时，该域为 y=26…1971；梯度所需的
两个真实像素在 y=5…1993 仍可观测。部分原图材料变化因此尚未进入峰测量，不能将缺测当成画面外侧。
峰的半高区间若真正被 source/query 边缘截断，仍没有精确中点；恢复测量坐标不自动恢复定位权限。

按源审查 106 个扫描源、212 条共享 H 指导线，7 条线上存在距 source 边缘不足 32 px 的源内研究点，
涉及 S100、S101、S102、S103、S105、S106。S101、S102、S106 还包含实际源外点，必须与源内窗口
容不下的点分别统计。这里的 32 px 仅描述研究点距源边的分布，不是新的运行时窗口或接触标签。
凭据为 `Test/gold_analysis/h_contact_dense_20260921/source_clearance_audit.json`。

离线窗口实验覆盖上述 6 源与用户判读图对应的 4 源，共 10 个原始扫描源。实验复用当前 registered
gray、baseline、走廊切片与峰 owner；原窗口的全部峰坐标和各通道统计逐项精确复现对应完整 CLI 报告。
统一将窗口缩为一半或四分之一，可以恢复部分源边证据，但也改变已测区域的统计归一化及峰划分，
不适合作为普遍替换。按真实源内可用长度限制双侧窗口的实验能在 S102 恢复分布更广的原始证据；
同时 S103 在实验所设近黄金研究窗口内没有新增满足现有信号与纹理方向条件的点；窗口外峰仍未判定，
不能仅凭距离称其为错误接触。靠源边的小窗口也可能仅含数个像素。
这些是可观测性和敏感性结果，不是接触正确率、空间覆盖资格或 H 几何验收。

只有证明能改善具体最终输出后，才考虑窗口集成；届时须在同一测量 owner 内显式区分各通道有效域、
实际材料窗口与定位完整性，缺测不得填零后
参与外侧平滑度比较。归一化总体、原 trace 分母、实际工作量和报告回链必须一并核对。不能通过模型
预测位置、峰强度或拟合结果选择有利窗宽，也不能忽略原始竞争解释后授予唯一解。
完整实验与局限位于 `source_edge_window_summary.json`，均为开发数据，未增加运行时检测路径。

新增 S100/S101/S103/S105/S106 的正式完整 CLI 诊断已通过当前报告校验并核对源 SHA，绑定当时现场
源码身份（包含尚未验收的修改），结果位于
`Test/debug_analysis/h_source_clearance_20260921/audit.json`。五源均保持 Review；四源缺少 H 角色权限，
S103 已有上下角色证据但不能覆盖所选模板范围。不能把这些不同缺口都归为窗口漏测，也不能把原始峰
增加当作裁切改善。本项尚无最终输出改善证明，不能作为运行时集成依据，也不构成 H 或性能验收。

## 正反例与验收顺序

先按源比较黄金边附近的特征分布和不同空间位置，再检查邻近竞争解释。相对冻结基准的方向性越界
是现有 accuracy 合同下的几何差异，不能直接转换为真实画面侵入或接触错误标签。内外偏移都先作为
未判定比较位置；只有独立原图证据或明确人工判读支持时，才赋予相应局部研究标签。灰尘、片夹边、内部强线、
突出点和低反差边界需要结合原图有界邻域及原始几何逐项判读，不能仅凭特征高低自动命名。
语义尚不清楚的例子保留为未判定，研究判读不修改人工 reference。
既有特征排序统计仅描述冻结基准与偏移位置的数值差别，不是正负例区分率；仅由偏移方向推断的
接触负标签须撤销并重新评价，不能通过增加运行时容差或样片特例消除差异。

研究始终分开三个层次：黄金保护线指导离线搜索；原图像素支持高精度真实接触位置及其不确定性；
最终裁切输出仍按既有黄金包含与外扩预算合同验收。真实接触线不自动等于最终裁切线。运行时不得
读取黄金坐标来选边或逐样片补偿；边界识别准确与输出满足保护约束必须分别验证。

特征评估同时保留弱正例和强反例，按源汇总，避免把同源大量窗口当成独立成功次数。单特征有区分
趋势不等于可以授权边界；必须检查特征组合在材料变化、缺测、短证据、杂点与竞争解释下的失败情况。

H 的接触证据需要进入同一位置、倾斜、上下配对、有界高度和空间覆盖求解。模型缩小可行域，像素
提供独立证据；未解决的 raw 解释必须传到联合求解，不能靠删除歧义点或退回单侧高度推断变成唯一解。
经真实样片及反例验证后，才替换现机制中被覆盖的路径，并验证完整生产 CLI 的准确性、安全性和回归。
H 验收前不进行 W 运行时改造。W 后续还须分别检查每张照片的边归属以及 separator、contact、overlap、
partial exposure 和 source truncation；同一 contact 物理线可有两个相反的内侧角色。

本工具只测量、记录研究数据，不增加运行时检测器，不调整 Gate、reference、格式先验或发布条件。
正式性能验收仍留到发布阶段。
